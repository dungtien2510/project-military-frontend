import React from "react";
import ListRelative from "../../components/relative/list/ListRelative";

const ListPage = () => {
  return (
    <>
      <ListRelative />
    </>
  );
};

export default ListPage;
export async function loader({ request }) {}
